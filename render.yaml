services:
  - type: web
    name: sathyathi-digital
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: MYSQL_URI
        sync: false
